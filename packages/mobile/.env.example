# API server origin (same as web NEXT_PUBLIC_API_URL). Port 3002 = API; 3001 = web app.
# Use your machine's LAN IP when testing on a physical device (localhost only works in simulator).
EXPO_PUBLIC_API_URL=http://localhost:3002
