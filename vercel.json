{
  "installCommand": "corepack enable && corepack prepare yarn@4.12.0 --activate && yarn -v && yarn workspaces focus @ops/shared @ops/web --production",
  "buildCommand": "yarn workspace @ops/shared build && yarn workspace @ops/web build",
  "outputDirectory": ".next"
}
